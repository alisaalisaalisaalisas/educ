apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-city-explorer.fullname" . }}
  labels:
    {{- include "devops-city-explorer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-city-explorer.selectorLabels" . | nindent 4 }}
